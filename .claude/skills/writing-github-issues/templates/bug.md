<!--
Bug body template. See
.claude/skills/writing-github-issues/SKILL.md for the choreography.

A bug describes unintended behaviour that needs fixing. Use this
template for both standalone bug reports and for sub-issues of an
epic that fence off a regression discovered during the epic's work.

Label: `bug`.
-->

# <plain-english-title>

<!-- Title rule: a human-readable sentence a no-context reader can
parse. Example: "Walker store drops sibling sessions across MCP
restart". -->

## Problem

<!-- A few sentences. What's broken (observed behaviour), in what
surface, and why it matters (operational impact, severity, blast
radius). No solution; the fix belongs in the PR that lands it. -->

<!-- optional -->

## Steps to reproduce

<!-- Numbered steps a maintainer can follow to reproduce the bug on a
fresh checkout (or a documented session). Include only when the bug
isn't trivially observable from Problem; omit otherwise. -->

## Expected behaviour

<!-- One or two sentences describing what should happen instead. -->

## Acceptance criteria

<!-- Bulleted, verifiable conditions a reviewer can answer "yes / no"
against at done-time. Each bullet names a concrete, observable
change. -->

<!-- optional -->

## Context

<!-- Links and pointers that help a no-context reader: the
investigation session id, the offending file path or function, prior
related bugs, log excerpts. Omit the section if there's nothing
worth linking. -->
